{% extends 'base.html.twig' %}

{% block title %}Famille{% endblock %}

{% block monTitre %}Découvre la famille des {{famille.libelle}}{% endblock %}

{% block body %}
    <div class="p-3">{{famille.description}}</div>

    <div class="row">
        {% for animal in famille.animaux %}
            <div class="col-6">
                <div class="row align-items-center">
                    <div class="cole-12 col-md-6 col-lg-2 text-center">
                        <img class="img-fluide" src="{{ asset('images/' ~ animal.image) }}" alt="">
                    </div>
                    <div class="col text-center">
                        <h2><a href="{{path('animal',{'id' : animal.id})}}">{{ animal.nom }}</a></h2>
                        <div>{{ animal.description}}</div>
                        <div><a href="" class="btn btn-info mt-2">{{animal.famille.libelle}}</a></div>
                    </div>
                </div>
            </div>                       
        {% endfor %}
    </div>


{% endblock %}
